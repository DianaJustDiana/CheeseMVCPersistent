@model List<CheeseMVC.Models.CheeseCategory>


<h1>@ViewBag.Title</h1>
<ul>

    @foreach (var cheeseCategory in Model)
        {
            <li>cheeseCategory</li>
        }
</ul>
@if (Model.Count == 0)
{
    <p>No cheese categories yet!</p>
}
